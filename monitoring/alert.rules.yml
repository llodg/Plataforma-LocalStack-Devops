groups:
  - name: app-alerts
    rules:

      - alert: HighRequestRate
        expr: app_requests_total > 10
        for: 10s
        labels:
          severity: warning
        annotations:
          description: "High number of requests detected"